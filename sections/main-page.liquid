<article class="container section" style="max-width:760px">
  <h1 style="font-family:var(--serif);font-weight:300;font-size:clamp(2rem,5vw,3.5rem);margin-bottom:32px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</article>
{% schema %}{ "name": "Page" }{% endschema %}